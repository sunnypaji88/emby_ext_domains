services:
  emby-domains:
    build: .
    container_name: emby-domains-api
    ports:
      - "52143:52143"
    volumes:
      - ./config.yaml:/app/config.yaml:ro
    restart: unless-stopped
    environment:
      - TZ=Asia/Shanghai